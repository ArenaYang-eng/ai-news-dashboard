# 專案交接摘要

最後更新：2026-09（依對話內容整理）

目前總共有三個獨立系統，全部基於「GitHub Actions 排程 + Google 試算表 + Apps Script + Groq 免費 AI」這套免費架構。以下依專案分別整理現況、架構、已知問題與修法、待辦事項。

---

## 專案一：AI 新聞蒐集系統（最早建立、最成熟）

### Repo / 資料位置
- GitHub repo：`AI-news-tracker`（Private）
- Google 試算表：`AI_News_Tracker`，分頁 `AI_News`
- GitHub Secrets：`GROQ_API_KEY`、`SCRIPT_URL`、`SCRIPT_TOKEN`

### 運作方式
- `weekly.yml`：每週一 UTC 00:00（台灣時間週一早上 8 點）自動執行 `main.py`
- Apps Script 的 `generateWeeklyReport` 設定時間觸發條件在台灣時間早上 9-10 點（比抓新聞晚一點，確保資料先寫入）

### 資料來源（FEEDS 清單）
涵蓋美國（OpenAI、Hugging Face、Berkeley BAIR、MIT News）、台灣、中國大陸（機器之心、量子位、Google News 簡中）、英國/歐洲（Google DeepMind）、日本、韓國、印度、以色列、中東。每個來源都有明確的 `region` 欄位標記。

### 資料欄位（試算表欄位順序）
```
date, title, url, source, region, summary_zh, tags
```
- `region` 是後來才加的獨立欄位，**在此之前累積的舊資料這欄是空的**，屬正常現象，之後新資料會逐漸把「未標示」比例稀釋掉
- `summary_zh` 尾端會帶括號標註類別，例如「...（落地應用）」或「...（研究/發布/政策）」，Streamlit 儀表板靠正則表達式解析這個括號取得類別

### 已知問題與修法（時間序）
1. **Groq 模型下架導致全部 404**：`GROQ_MODEL` 原本是 `llama-3.3-70b-versatile`，2026/8/16 被 Groq 官方下架，已改成 `openai/gpt-oss-120b`。**這個模型未來也可能被下架**，如果又遇到大量 404，去 `console.groq.com/docs/deprecations` 查目前可用模型並更新這個變數
2. **全軍覆沒無聲失敗**：加了 `send_alert()`機制，當「候選文章數 > 0 但成功處理數 = 0」時，透過 Apps Script 寄警示信到信箱
3. **寫入試算表偶發連線逾時**：`push_to_sheet()` 內建自動重試（最多 3 次，間隔 5s/10s 漸進拉長）
4. **週報顯示 0 則但資料明明存在**：原因是 `Code.gs` 裡 `SHEET_NAME` 跟試算表實際分頁名稱對不上，且原本程式碼會「默默」改用 `getActiveSheet()` 掩蓋掉這個錯誤。已修正為找不到指定分頁就直接拋出錯誤，不會誤判成 0 筆

### 待辦 / 尚未做的事
- 目前只有這個專案接了 Streamlit 儀表板（見專案三），輿情監控那邊還沒有視覺化
- `region` 欄位的歷史空值問題，等資料自然累積即可，不需要特別處理

---

## 專案二：機關輿情監控系統（多機關版本，目前規劃 6-8 個機關）

### Repo / 資料位置
- GitHub repo：`agency-opinion-monitor`（Private）
- Google 試算表：目前用同一份試算表、依機關分頁（例如 `水利署`、`中水分署`）
- GitHub Secrets：`GROQ_API_KEY`、`SCRIPT_URL`、`SCRIPT_TOKEN`（跟 AI 新聞系統是**各自獨立**的一組，但可以共用同一個 Groq 帳號的金鑰）

### 架構演進
最初是單一機關（水利署）雛形，後來重構成「設定檔驅動」的多機關架構：
- `main.py`：共用邏輯，不用因為新增機關而修改
- `configs/*.json`：一個檔案代表一個監控對象，目前有 `shuili.json`（水利署）、`wracb.json`（中區水資源分署/中水分署）
- 中央社分類 RSS（地方/社會/政治）是**所有機關共用**的來源，一次執行只抓一次，各機關各自從裡面篩選，節省重複抓取
- `Code.gs` 的 `MONITORED_SHEETS` 陣列列出目前有在監控的分頁名稱，`doPost` 依 `obj.sheet_name` 動態寫入對應分頁

### 設定檔（config）欄位說明
```json
{
  "sheet_name": "分頁名稱",
  "agency_name": "核心比對關鍵字",
  "keywords_for_search": ["Google News 搜尋用的關鍵字組合"],
  "confusable_names": ["容易混淆的同名機關全名，會被排除"],
  "major_outlets": {"媒體名稱": "網域"}
}
```
- **設定檔檔名務必用英文**：曾經用中文檔名（`水利署.json`）導致 Git 編碼衝突、無法正常 commit，改用英文檔名（如 `shuili.json`）後解決
- **JSON 語法要注意全形符號**：中文輸入法容易把逗號、引號自動轉成全形（，""），造成 JSON 解析錯誤，建議打符號時切換成英文輸入法，或直接複製既有範本修改文字內容
- 新增機關的完整流程：① 試算表新增對應分頁（表頭：`date, title, url, source, sentiment, tags`，**注意目前這個系統的表頭跟 AI 新聞系統不同，沒有 `region` 欄位**）② `MONITORED_SHEETS` 陣列加上分頁名稱 ③ `configs/` 資料夾新增設定檔 ④ 不用改 `main.py`、`daily.yml`

### 情緒判讀與同名機關混淆處理
- 摘要 prompt 除了摘要跟標籤，新增「輿情傾向」欄位（正面/中性/負面）
- `CONFUSABLE_NAMES` 機制：例如「農田水利署」「農田水利會」容易被誤判成「水利署」（經濟部），比對前會先把這些名稱從文字中挖掉再判斷，這個檢查套用在**所有來源**（不只中央社，Google News 搜尋一樣會有這個問題，因為是字串包含比對）

### 去重機制（分兩層，經過兩輪修正才完整）
1. **同次執行內去重**：比對「標準化後的標題」（去除所有空白差異），不能只靠比對網址，因為 Google News 給的網址每次都帶不同追蹤碼
2. **跨執行去重（更關鍵）**：因為 `ONLY_RECENT_DAYS = 2`，同一篇新聞可能連續兩三天的執行都判定為「近期」而重複抓取。這層去重無法在 Python 端完成（每次執行都是全新、沒有記憶），改成在 **`Code.gs` 的 `doPost` 寫入前，先掃描該分頁現有資料的標題**，比對到重複就回傳 `Duplicate skipped`、不寫入。`main.py` 也配合更新，把這個回應視為正常結果（不當作失敗重試），log 會顯示「跳過（近期已寫過）」

### 已知問題與修法
1. 同 AI 新聞系統：Groq 模型下架問題、Token 不一致問題、寫入逾時重試機制、GitHub 排程超過 60 天無 commit 會被自動關閉
2. **資料來源過於單一**：原本 `source` 欄位對 Google News 來源都寫死「Google News (關鍵字)」，看起來像只有單一來源。已修正為解析 Google News RSS 裡真實的 `<source>` 標籤或標題後綴，取得真實媒體名稱（自由時報、聯合報、中央社等）
3. **中央社分類 RSS 命中率太低**：分類新聞量大，含特定機關關鍵字的比例低，原本只掃 15 則常常整批篩到 0 篇。已調高到 60 則
4. **新增機關時 Code.gs 意外重置 Token**：曾經整份覆蓋 `Code.gs` 時忘記把 `SECRET_TOKEN` 改回真實值，導致全部寫入變成 `Unauthorized`。**教訓：每次覆蓋 Code.gs 後，務必檢查 `SECRET_TOKEN`、`NOTIFY_EMAIL` 是否還是原本的真實值，而不是範本裡的預留文字**
5. **設定檔誤放根目錄**：GitHub 網頁版 Upload files 只會放到目前瀏覽的資料夾層級，曾發生新設定檔被放到 repo 根目錄而非 `configs/`，導致程式讀不到。上傳前記得先點進 `configs/` 資料夾內部再上傳，或上傳後用「編輯檔名」的方式把路徑改成 `configs/檔名.json`

### 目前規模與觀察期
- 目標規模：6-8 個機關/議題
- 目前只上線 2 個（水利署、中水分署），處於**觀察期**，尚未大量擴充，原因：
  - 確認資料品質（關鍵字準不準、有沒有新的同名混淆狀況）
  - 確認 Groq 免費額度夠不夠用（規模擴大後 tokens 用量會上升）
  - 建議至少累積 2-3 週乾淨資料再考慮做視覺化或大量擴充

### 待辦 / 尚未做的事
- **尚未加入 `region` 獨立欄位**（AI 新聞系統已經加了，這個系統還沒有同步做，如果之後需要跨機關/跨地區統計，可以比照辦理）
- **尚未做視覺化儀表板**：因為資料屬於機關輿情、較敏感，之前討論過 **Tableau Public 不適合**（免費方案資料會公開在網路上），建議用 Looker Studio（分享權限設定為「限定同事帳號」而非「知道連結即可看」）或 Streamlit（本機部署、不對外開放網址，比照使用者另一個 `geo_app` 專案的本機部署模式）
- 擴充到 6-8 個機關時，記得分散排程時間或觀察 Groq 用量，避免額度吃緊

---

## 專案三：AI 新聞 Streamlit 儀表板

### Repo / 資料位置
- GitHub repo：`ai-news-dashboard`（Private，但 Streamlit 部署出的網址是公開可看的）
- 部署平台：Streamlit Community Cloud（`share.streamlit.io`）
- 已成功部署上線

### 運作方式
- 資料來源：讀取 AI 新聞試算表的**公開 CSV 匯出連結**（試算表共用設定為「知道連結的任何人可檢視」），不需要申請任何 Google API 憑證
- Streamlit Secrets 設定：`SHEET_ID`、`SHEET_NAME`
- 資料每 30 分鐘自動重新讀取一次（`@st.cache_data(ttl=1800)`）

### 檔案結構
- `dashboard.py`：主程式，含資料讀取、清理、篩選器、圖表、資料表格
- `requirements.txt`：`streamlit`, `pandas`, `plotly`
- `SETUP_GUIDE.md`：部署步驟說明

### 畫面內容
- 頂部 KPI 卡片：符合篩選條件文章數、涵蓋地區數、落地應用篇數、平均每週篇數
- 側邊篩選器：日期範圍、地區（多選）、類別（多選）、關鍵字搜尋
- 三個分頁：
  1. **趨勢與分布**：每週文章量趨勢圖（面積圖）、落地應用 vs 研究類別圓餅圖、各地區文章數長條圖
  2. **主題標籤**：熱門標籤 Top 15 長條圖
  3. **文章清單**：可篩選、可排序表格，附原文連結

### 部署過程踩過的坑
- **repo 沒有出現在 Streamlit 部署下拉選單**：原因是 Streamlit 的 GitHub App 授權範圍設定成「只允許特定 repo」，新建的 repo 沒有被加進授權清單。解法：去 `github.com/settings/installations` 找到 Streamlit 應用，把新 repo 加進 Repository access；或者直接用部署畫面的「Paste GitHub URL」功能繞過下拉選單
- **`region` 欄位有大量「未標示」**：因為是舊資料（加欄位之前累積的），非程式錯誤，會隨新資料累積自然改善

### 待辦 / 尚未做的事
- 使用者提出想調整版面配置/呈現方式，尚未收到具體需求，等後續指示再動手調整。可能的調整方向已先整理成對照表供參考：圖表順序、拿掉/新增圖表、KPI 內容、篩選器位置（側邊欄 vs 主畫面橫向排列）、配色（`COLOR_SEQUENCE` 變數）、快取時間（`ttl` 參數）
- 可考慮把「未標示」從地區長條圖排除，只顯示有明確地區標記的資料，畫面會更乾淨（已提出但未實作，等使用者確認要不要做）

---

## 整體 Roadmap 回顧（討論過、部分尚未執行）

1. **資料集中化**（已完成，僅限輿情監控系統）：從「一機關一 repo」改成「設定檔驅動、共用試算表分頁」
2. **視覺化儀表板**：
   - AI 新聞系統：✅ 已完成（Streamlit）
   - 輿情監控系統：❌ 尚未開始，需先決定 Looker Studio（權限設嚴格）或 Streamlit 本機部署，因資料敏感不能用 Tableau Public
3. **跨主題統一警示**（例如一封信彙整所有監控主題當日重點，或串 LINE Notify）：討論過，尚未實作
4. **GitHub AI/LLM 專案追蹤**：討論過利用同一套架構延伸監控 GitHub 上的新專案，評估後認為**手動看 `github.com/trending` 可能比自動化更划算**，除非有窄主題的深度追蹤需求。目前**沒有動手做**，只是分析與建議，供之後參考

---

## 通用故障排查速查表（三個系統共通）

| 現象 | 原因 | 解法 |
|---|---|---|
| `AssertionError: 缺少環境變數` | GitHub Secrets 沒設定或名稱打錯 | 檢查 Settings → Secrets and variables → Actions |
| Groq API 回傳 `404` | `GROQ_MODEL` 指定的模型被下架 | 查 `console.groq.com/docs/deprecations`，更新模型名稱 |
| 寫入試算表 `Unauthorized` | GitHub Secrets 的 `SCRIPT_TOKEN` 跟 `Code.gs` 的 `SECRET_TOKEN` 不一致 | 兩邊重新設成同一組值 |
| 簡報/週報顯示 0 則但資料存在 | `Code.gs` 的 `SHEET_NAME`（或 `MONITORED_SHEETS`）跟試算表實際分頁名稱對不上 | 確認分頁名稱完全一致 |
| 同一則新聞重複出現 | 同次執行內標題比對沒過濾掉、或跨執行重複抓到舊新聞 | 確認兩層去重機制都有生效（標題正規化 + Apps Script 寫入前掃描既有資料） |
| 抓到不相關機關的新聞 | 同名機關混淆（例如農田水利署 vs 經濟部水利署） | 在該機關的 `CONFUSABLE_NAMES`（或程式對應清單）加入容易混淆的全名 |
| GitHub Actions 排程忽然沒有自動執行 | repo 超過 60 天無 commit，GitHub 自動關閉排程 | Actions 分頁找「workflow disabled」提示，點 Enable |
| Apps Script 修改後沒生效 | 只存檔、沒有重新部署 | 部署 → 管理部署作業 → 編輯 → 新版本 → 部署 |
| JSON 設定檔解析錯誤 | 中文輸入法把逗號/引號打成全形 | 打符號時切換英文輸入法，或複製既有範本只改文字 |
| 中文檔名無法 commit | Git 對中文檔名的編碼處理問題（尤其 Mac） | 設定檔一律用英文檔名，內容裡的中文不受影響 |
| Streamlit 部署找不到 repo | GitHub App 授權範圍沒涵蓋該 repo | `github.com/settings/installations` 調整權限，或用「Paste GitHub URL」 |

---

## 三個系統共用的技術棧

- **執行環境**：GitHub Actions（免費排程執行 Python）
- **資料儲存**：Google 試算表
- **中介層**：Google Apps Script（接收資料寫入試算表、產生報告文件、寄信）
- **AI 摘要/分析**：Groq API，模型 `openai/gpt-oss-120b`（免費額度：約每天 1,000 次請求、每天 200,000 tokens，無需信用卡）
- **視覺化**：Streamlit Community Cloud（AI 新聞已完成），Looker Studio 待評估（輿情監控）
